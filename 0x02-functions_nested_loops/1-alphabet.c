#include "main.h"

/**
 * main - Entry point
 *
 * Description: prints from a to z using putchar prototype
 *
 * Return: 0 (Success)
*/

void print_alphabet(void)
{
	int ch;

	for (ch = 'a'; a <= 'z'; ch++)
		_putchar(ch);
	_putchar('\n');
}
